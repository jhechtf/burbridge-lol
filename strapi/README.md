# Strapi

The Strapi API service that helps us build our website.

## Local Seed Data

Import with 

```
pnpm strapi import -f seed-data.tar.gz
```

## Running Locally

Run with `pnpm dev` in the `/strapi` directory, then in a new terminal
run `pnpm dev` in the main main folder as well.
